// El unshift() método de Array instancias agrega los elementos especificados al comienzo de una matriz y devuelve la nueva longitud de la matriz.


// Resumen 
// Agrega nuevos elementos al comienzo de una matriz y devuelve la nueva longitud

const array1 = [1, 2, 3];

console.log(array1.unshift(4, 5));
// Expected output: 5

console.log(array1);
// Expected output: Array [4, 5, 1, 2, 3]

